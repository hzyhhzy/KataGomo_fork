# 优化历史

## 1. 基线

### 1.1 测试环境
- GPU: NVIDIA GeForce RTX 5080
- TensorRT: 10.15
- 锁频策略: GPU/MEM = `2550 / 14801`
- 说明: 使用相对保守的锁频，避免撞功耗/温度墙造成频率漂移，影响测量精度。

### 1.2 测试方法
1. 先运行 `benchmark.sh` 获取官方推荐线程规模。
2. 再使用 `run.sh --benchmark` 在固定参数下做单独测量。

### 1.3 官方推荐线程数
```text
2026-03-05 21:50:13+0800: TensorRT backend thread 0: Model version 15 useFP16 = true
numSearchThreads = 32: 10 / 10 positions, visits/s = 3209.53 nnEvals/s = 2885.87 nnBatches/s = 188.00 avgBatchSize = 15.35 (2.6 secs) (EloDiff +316) (recommended)
```
结论: 官方 benchmark 推荐 `numSearchThreads = 32`。

### 1.4 run.sh --benchmark 测量结果
```text
2026-03-05 21:53:58+0800: TensorRT backend thread 0: Model version 15 useFP16 = true
numSearchThreads = 32: 10 / 10 positions, visits/s = 4504.67 nnEvals/s = 2957.98 nnBatches/s = 185.61 avgBatchSize = 15.94 (22.3 secs) (EloDiff baseline)
```

### 1.5 基线结论
- 推荐线程: `numSearchThreads = 32`
- 基线指标（`run.sh --benchmark`）:
  - `visits/s = 4504.67`
  - `nnEvals/s = 2957.98`
  - `nnBatches/s = 185.61`
  - `avgBatchSize = 15.94`

## 2. 参数调整

### 2.1 测试范围
- 对 `batch=1..32` 每个尺寸分别构建 plan cache。
- 使用 `trtexec` 在不同 `cudaStream` 下测试吞吐表现。
- python python/benchmark.py 输出结果见:
  - `benchmark/trtexec_benchmark_b18tf.onnx_gpu-NVIDIA_GeForce_RTX_5080.json`
  - `benchmark/trtexec_benchmark_b18tf.onnx_gpu-NVIDIA_GeForce_RTX_5080.png`
  
![trtexec benchmark](benchmark/trtexec_benchmark_b18tf.onnx_gpu-NVIDIA_GeForce_RTX_5080.png)

### 2.2 关键结论
- `batchsize` 必须是 `8+4k` 的限制没有收益，因此移除。
- 由此修改了 `cpp/command/benchmark.cpp` 中对应限制逻辑。
- 参数选择上，最终采用 `batch=7, cudaStream=2`。
- 从观测结果看，更高 `cudaStream` 没有带来稳定更高收益。

### run.sh --benchmark 结果。
```text
numSearchThreads = 21: 10 / 10 positions, visits/s = 5623.56 nnEvals/s = 3780.92 nnBatches/s = 555.69 avgBatchSize = 6.80 (17.8 secs) (EloDiff baseline)
```

### 2.3 numSearchThreads 选择
- 选择规则: `numSearchThreads = (numNNServerThreadsPerModel + 1) * batchsize`
- 当前参数下: `numNNServerThreadsPerModel = 2`，`batchsize = 7`，因此 `numSearchThreads = 21`。
- 解释:
  - 2 组线程负责等待 GPU 推理返回（对应 2 个 CUDA stream）。
  - 额外 1 组线程负责持续生成新请求。
  - 目标是在推理和请求生成之间流水化，尽量填满 GPU。

## 3. cudaGraph 实现与验证

### 3.1 为什么要改这么多 backend
- `trtUseCudaGraph` 是 TensorRT 侧配置，但它从配置文件流入推理执行层时，要经过统一的 `NNEvaluator -> NeuralNet::createComputeContext` 接口链路。
- 如果继续用“单独加一个 bool 参数”的方式，后续每新增一个 TRT 选项都要再次修改所有 backend 的函数签名，维护成本高。
- 因此本次先引入 `TRTConfigs` 结构体，所有 backend 一次性完成签名升级。后续再加 TensorRT 选项时，只改 `TRTConfigs` 字段和 TRT backend 逻辑。

### 3.2 cudaGraph 实现细节
- 开关来源: `trtUseCudaGraph`（配置文件和 `run.sh` 的 override-config）。
- 捕获策略: 按 `batchSize` 预捕获 `1..maxBatchSize`，每个 batch 保存一份 graph/graphExec。
- 存储结构: `batchGraphStates` 使用连续 `vector`（按 batch 下标访问），不使用 `map`。
- 启动并发保护: 预捕获阶段加全局互斥，避免多线程同时 capture 导致 CUDA/Myelin 竞争错误。
- 推理路径:
  - 关闭 `trtUseCudaGraph` 时走原始 `enqueueV3`。
  - 开启 `trtUseCudaGraph` 时按当前 batch 直接 `cudaGraphLaunch` 预捕获结果。

### 3.3 对齐上次参数（t=21, b=7, s=2）
```text
numSearchThreads = 21: 10 / 10 positions, visits/s = 5842.03 nnEvals/s = 3772.92 nnBatches/s = 718.58 avgBatchSize = 5.25 (17.2 secs) (EloDiff baseline)
```

观察:
- `nnEvals/s` 与上一轮接近，但 `avgBatchSize` 明显下降到 `5.25`。
- 这说明当前 `numSearchThreads=21` 下，batch 聚合不充分，搜索线程出现不饱和。

### 3.4 补充测试（numSearchThreads=28）
开启 cudaGraph:
```text
numSearchThreads = 28: 10 / 10 positions, visits/s = 6421.56 nnEvals/s = 4127.33 nnBatches/s = 591.46 avgBatchSize = 6.98 (15.6 secs) (EloDiff baseline)
```

关闭 cudaGraph:
```text
numSearchThreads = 28: 10 / 10 positions, visits/s = 6066.72 nnEvals/s = 3990.65 nnBatches/s = 571.78 avgBatchSize = 6.98 (16.5 secs) (EloDiff baseline)
```

结论:
- `numSearchThreads` 提升到 `28` 后，吞吐显著提升。
- 在同样 `t=28,b=7,s=2` 下，开启 cudaGraph 的 `nnEvals/s` 高于关闭 cudaGraph，提升明显。

## 4. 显式 cuda stream 拷贝与 pinned memory

### 4.1 实现内容
- `InputBuffers` 的输入/输出 host buffer 改为 pinned host memory。
- H2D / D2H 全部显式绑定到 `cudaStreamPerThread`，避免默认 stream 干扰推理流水。
- D2H 改为异步拷贝，最后统一 `cudaStreamSynchronize`，再进入 CPU 侧后处理。

### 4.2 与第 3 节同参数对照（t=21, b=7, s=2, cudaGraph=on）
第 3 节中的结果:
```text
numSearchThreads = 21: 10 / 10 positions, visits/s = 5842.03 nnEvals/s = 3772.92 nnBatches/s = 718.58 avgBatchSize = 5.25 (17.2 secs) (EloDiff baseline)
```

加入显式 copy stream 与 pinned memory 后:
```text
numSearchThreads = 21: 10 / 10 positions, visits/s = 6277.29 nnEvals/s = 4142.30 nnBatches/s = 634.88 avgBatchSize = 6.52 (16.0 secs) (EloDiff baseline)
```

结论:
- 在保持 `cudaGraph=on` 不变的前提下，吞吐继续提升。
- `visits/s`: `5842.03 -> 6277.29`
- `nnEvals/s`: `3772.92 -> 4142.30`

### 4.3 进一步测试（t=28, b=7, s=2, cudaGraph=on）
```text
numSearchThreads = 28: 10 / 10 positions, visits/s = 6550.22 nnEvals/s = 4209.86 nnBatches/s = 603.22 avgBatchSize = 6.98 (15.3 secs) (EloDiff baseline)
```

结论:
- `stream copy + pinned memory` 在第 3 节的 `cudaGraph` 基础上进一步抬高了吞吐。
- 当前最高点是 `t=28,b=7,s=2,trtUseCudaGraph=true`。

## 5. globalPerfProfile

### 5.1 为什么需要 profiling
- 到第 4 节为止，参数调整、`cudaGraph`、显式 `cuda stream` 拷贝和 pinned memory 这些容易想到且收益明确的优化已经做完了。
- 再往下继续优化，已经不能只凭经验猜测，而需要知道时间究竟花在搜索线程、等待队列、推理线程还是 GPU 本身。
- 因此新增 `globalPerfProfile`，为下一轮调度优化提供定量依据。

### 5.2 修改内容
- 新增全局配置项 `globalPerfProfile`，默认关闭。
- `run.sh --benchmark` 默认打开该选项。
- 输出时机放在 benchmark 末尾、`GPU ... finishing` 之后，避免和正常 benchmark 输出交错。
- 所有统计量都按 sample 级别修正: 每个 benchmark sample 单独裁掉头尾各 `100ms`，避免把 sample 启动/收尾阶段误判成稳态行为。
- 本次新增的统计项包括:
  - 搜索线程单次循环的 `search_process_ms` 和 `search_wait_nn_ms`
  - 等待队列长度时间占比 `queue_length_time_share`
  - 推理线程分段耗时: `inference_preprocess_ms`、`inference_h2d_ms`、`inference_wait_gpu_ms`、`inference_d2h_ms`、`inference_postprocess_ms`
  - 同 GPU 连续 launch 间隔的十分位分布: `inference_launch_interval_ms`
  - 推理线程活跃度时间占比: `inference_thread_time_share`
  - GPU 按 batch size 的执行时间占比: `gpu_batch_time_share`

### 5.3 与第 4 节对齐的参数点
为了和第 4 节对照，本节继续使用 `t=21, b=7, s=2, trtUseCudaGraph=true`。

```text
numSearchThreads = 21: 10 / 10 positions, visits/s = 6341.16 nnEvals/s = 4084.83 nnBatches/s = 660.60 avgBatchSize = 6.18 (15.8 secs) (EloDiff baseline)

globalPerfProfile
  search_process_ms: P50=0.060 P95=0.142 P99=0.198
  search_wait_nn_ms: P50=3.878 P95=5.992 P99=6.231
  queue_length_time_share: q0=3.27%, q1=1.88%, q2=1.88%, q3=1.88%, q4=1.82%, q5=2.29%, q6=2.74%, q7=45.35%
    q8=4.06%, q9=9.47%, q10=10.70%, q11=8.95%, q12=5.65%, q13=0.05%
  inference_preprocess_ms: P50=0.012 P95=0.018 P99=0.020
  inference_h2d_ms: P50=0.013 P95=0.019 P99=0.022
  inference_wait_gpu_ms: P50=3.197 P95=3.297 P99=3.313
  inference_d2h_ms: P50=0.007 P95=0.013 P99=0.017
  inference_postprocess_ms: P50=0.002 P95=0.003 P99=0.003
  inference_launch_interval_ms: D10=0.025 D20=0.036 D30=0.506 D40=0.688 D50=1.650 D60=2.385 D70=2.610 D80=2.723 D90=2.833
  inference_thread_time_share: active0=0.00%, active1=0.01%, active2=99.99%
  gpu_batch_time_share: b1=0.05%, b2=3.63%, b3=5.42%, b4=6.13%, b5=5.28%, b6=2.11%, b7=77.37%
```

### 5.4 结论
- 由于所有统计都已经按 sample 去掉头尾各 `100ms`，这里的结果可以直接视为稳态观测。
- 搜索线程自身开销很小，主要时间仍然花在等待推理返回。
- `inference_thread_time_share` 几乎完全是 `active2=99.99%`，说明稳态下两条推理线程基本一直都在忙，不存在大段稳态空闲。
- 但 `active1` 仍然几乎为零，说明问题不是“GPU 没有活干”，而是“两条推理流始终几乎同相”，没有形成健康的错峰衔接。
- `queue_length_time_share` 中 `q7` 以上仍占绝大多数，`q9/q10/q11/q12` 也都有明显占比，说明请求生产和消费之间仍存在团簇化，而不是理想的平滑流水。
- `b7` 约 `77%`，剩余时间仍有相当一部分落在 `b2` 到 `b6` 的非满 batch；这说明即使在去掉头尾后的稳态口径下，调度仍然没有把 GPU 长时间稳定维持在满 batch 状态。

## 6. 满 Batch 调度

### 6.1 修改内容
- 调整推理调度策略：除非对应 GPU 处于空闲状态，否则只允许发起 `batchSize = maxBatchSize` 的推理。
- 当 GPU 空闲时，仍允许非满 batch 推理启动，以避免完全空转。
- 目标不是改变搜索逻辑，而是减少 GPU 上的非满 batch 执行时间占比。

### 6.2 与第 5 节对齐的参数点
继续使用 `t=21, b=7, s=2, trtUseCudaGraph=true`。

```text
numSearchThreads = 21: 10 / 10 positions, visits/s = 6405.91 nnEvals/s = 4200.39 nnBatches/s = 601.30 avgBatchSize = 6.99 (15.6 secs) (EloDiff baseline)

globalPerfProfile
  search_process_ms: P50=0.055 P95=0.128 P99=0.175
  search_wait_nn_ms: P50=3.981 P95=5.911 P99=6.104
  queue_length_time_share: q0=4.81%, q1=2.12%, q2=1.88%, q3=1.91%, q4=2.03%, q5=3.03%, q6=4.37%, q7=79.84%
    q8=0.00%, q9=0.00%
  inference_preprocess_ms: P50=0.012 P95=0.018 P99=0.020
  inference_h2d_ms: P50=0.013 P95=0.014 P99=0.017
  inference_wait_gpu_ms: P50=3.277 P95=3.310 P99=3.322
  inference_d2h_ms: P50=0.007 P95=0.013 P99=0.013
  inference_postprocess_ms: P50=0.002 P95=0.003 P99=0.003
  inference_launch_interval_ms: D10=0.683 D20=0.858 D30=1.033 D40=1.205 D50=1.714 D60=2.121 D70=2.294 D80=2.474 D90=2.643
  inference_thread_time_share: active1=0.04%, active2=99.96%
  gpu_batch_time_share: b7=100.00%
```

### 6.3 结论
- `avgBatchSize` 从第 5 节的 `6.18` 提升到 `6.99`，已经非常接近理论上限 `7.00`。
- `gpu_batch_time_share` 达到 `b7=100.00%`，说明稳态下 GPU 上已经基本不再执行非满 batch 推理。
- `nnEvals/s` 从 `4084.83` 提升到 `4200.39`，吞吐继续上升。
- `queue_length_time_share` 基本收敛到 `q7`，说明请求队列不再频繁穿越大量非满 batch 区间，而是更稳定地围绕一整批请求运行。
- `inference_thread_time_share` 仍然几乎完全是 `active2`，说明这次优化没有引入新的稳态空闲，改动主要体现在 batch 利用率提升上。

## 7. CUDA 同步模式开关

### 7.1 修改内容
- 新增 `trtCudaSyncMode` 配置项，支持 `spin|blocking|yield|auto`。
- 当前默认值设为 `blocking`。
- 该开关控制 host 线程在等待 CUDA 同步点时的行为，例如 `cudaStreamSynchronize`。
- 容器环境下，进程往往能看见很多逻辑核，但不一定真的拥有这些 CPU 配额，因此不能依赖 `auto` 模式的启发式判断。

### 7.2 与第 6 节对齐的参数点
继续使用 `t=21, b=7, s=2, trtUseCudaGraph=true`，分别测试 `spin` 和 `blocking`。

`spin`
```text
numSearchThreads = 21: 10 / 10 positions, visits/s = 6366.76 nnEvals/s = 4199.31 nnBatches/s = 601.23 avgBatchSize = 6.98 (15.7 secs) (EloDiff baseline)
TIME user=44.91 sys=0.71 cpu=239% elapsed=19.08
```

`blocking`
```text
numSearchThreads = 21: 10 / 10 positions, visits/s = 6301.60 nnEvals/s = 4204.38 nnBatches/s = 601.87 avgBatchSize = 6.99 (15.9 secs) (EloDiff baseline)
TIME user=16.64 sys=0.83 cpu=89% elapsed=19.57
```

### 7.3 结论
- `blocking` 相比 `spin`，总 CPU 时间从 `45.62s` 降到 `17.47s`，下降约 `61.7%`。
- 吞吐变化很小:
  - `visits/s`: `6366.76 -> 6301.60`，约 `-1.0%`
  - `nnEvals/s`: `4199.31 -> 4204.38`，基本持平，处于噪声范围内
- 对于 CPU 资源紧张的环境，`blocking` 明显更友好。
- 对于只追求最低等待延迟、且 CPU 资源充足的环境，`spin` 仍然更符合 CUDA 的传统默认用法。

## 8. TRT 配置项

### 8.1 配置项总表
- `trtBuilderOptimizationLevel`
  - TensorRT builder 内部参数。
  - 控制 builder 为寻找更优 tactic 所投入的整体搜索强度。
  - 默认值 `-1` 表示保持 TensorRT 默认行为，不调用对应 setter。

- `trtMaxAuxStreams`
  - TensorRT builder 内部参数。
  - 控制 engine 构建时允许使用的最大 auxiliary stream 数量。
  - 默认值 `-1` 表示保持 TensorRT 默认行为，不调用对应 setter。

- `trtAvgTimingIterations`
  - TensorRT builder 内部参数。
  - 控制 builder 在 tactic profiling 时做平均的 timing iteration 数。
  - 默认值 `-1` 表示保持 TensorRT 默认行为，不调用对应 setter。

- `trtTilingOptimizationLevel`
  - TensorRT builder 内部参数。
  - 控制 TensorRT 是否尝试片上缓存的 tiling 优化，以及搜索强度。
  - 可选值: `none|fast|moderate|full`

- `trtDeviceToUse`
  - 单 GPU 时选择默认设备。

- `trtDeviceToUseThreadN`
  - 多推理线程或多 GPU 时，按线程指定设备映射。

### 8.2 说明
- 本节只列出更依赖具体 GPU、驱动、TensorRT 版本和容器/宿主机环境的 TRT 配置项。
- 因此这些参数在文档里只作为开放配置项提供，不在这里给出固定 benchmark 结论。
- 如果要比较它们的效果，应该在固定 `batchSize`、`cudaStream`、`cudaGraph`、时钟和温度条件下单独做对照测试。

## 9. Simple Sampling

### 9.1 benchmark.py 工具改造
- `benchmark.py` 统一为一条主流程，不再区分单次 build 和专门的 simple-sampling 模式。
- 新增 `--build-count`，含义是“同一 `batchSize` 独立构建多少次 plan”。
- 新增 `--devices`，支持多张同型号 GPU 并行跑独立 build。
- 新增 `--home-data-dir-base`，每次 build 都使用隔离的 `homeDataDir/trtcache`，避免不同样本互相覆盖。

这次改造的目的不是改变 TRT 配置，而是把“多次独立生成同一模型并取最优”这件事变成一个稳定、可恢复、可多卡扩展的标准工作流。

如果要做一次固定配置的多卡并行 sampling，可以把 `batchSize` 和 `stream` 固定住，再配合 `--devices`、`--build-count`、`--home-data-dir-base` 一起使用。下面这个例子会在两张同型号 GPU 上并行生成 `64` 个 `batch=7, stream=2` 的 raw sample:

```bash
python python/benchmark.py \
  --devices 3,4 \
  --build-count 64 \
  --max-batch 7 \
  --batch-min 7 \
  --batch-max 7 \
  --stream-min 2 \
  --stream-max 2 \
  --home-data-dir-base benchmark/home_data_runs
```

`benchmark.py` 会把独立 build 分散到所选 GPU 上，并为每次 build 分配隔离的 `homeDataDir/trtcache`。

### 9.2 128 个 raw sample 的性能分布
- 固定参数: `batch=7, stream=2, cudaGraph=on`
- 一共保留 `128` 个互相独立生成的 raw engine 样本
- 汇总结果:
  - `mean = 4328.82 nnEval/s`
  - `median = 4330.00 nnEval/s`
  - `std = 38.04`
  - `min = 4189.35`
  - `max = 4427.51`
  - `P05/P95 = 4261.58 / 4396.41`

![Simple sampling histogram](benchmark/simple_sampling_raw128_hist_gpu-NVIDIA_GeForce_RTX_5080.png)

结论:
- 同样配置下，TensorRT builder 生成出来的 engine 存在明显的固有性能分布，不是单点结果。
- 最佳样本相对总体均值高出约 `98.69 nnEval/s`，约 `+2.28%`。
- 这说明 simple sampling 本身就值得做；很多时候不需要更复杂的搜索算法，单纯多 build 几次就能拿到更快的 engine。

### 9.3 同一 engine 重复跑的波动
- 对当前 simple-sampling 得到的最佳 raw engine，再单独用 `trtexec` 连续重复测试 `10` 次。
- 结果见 `benchmark/simple_sampling_best_engine_repeat10_gpu-NVIDIA_GeForce_RTX_5080.json`
- 汇总结果:
  - `mean = 4473.94 nnEval/s`
  - `min/max = 4455.24 / 4502.48`
  - `std = 15.15`

对比 128 个 raw sample:
- 跨 engine 的分布范围是 `4189.35 -> 4427.51`，总跨度约 `238.15`
- 同一 engine 的重复测试范围是 `4455.24 -> 4502.48`，总跨度约 `47.24`

结论:
- 运行期测量噪声当然存在，但远小于“重新编译生成不同 engine”带来的差异。
- 因此这里观察到的性能分布，主因仍然是编译期 tactic 选择不同，而不是 `trtexec` 测试噪声。

### 9.4 额外结论
- 之前还做过一个补充对照: 先锁定 GPU 主频编译，再回到不锁频环境下运行，性能反而会下降。
- 因此后续的 simple-sampling 流程不再依赖锁频编译，而是直接在实际运行条件下做 build 和筛选。
